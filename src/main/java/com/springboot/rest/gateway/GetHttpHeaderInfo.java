package com.springboot.rest.gateway;

import java.io.IOException;
import java.net.HttpURLConnection;
import java.net.URL;

import org.springframework.stereotype.Service;

@Service
public class GetHttpHeaderInfo {

	/*
	 * public static void main(String[] args) throws IOException { URL urlObj = new
	 * URL("http://httpbin.org/get"); HttpURLConnection conn = (HttpURLConnection)
	 * urlObj.openConnection();
	 * 
	 * // Accessing header info for(int i=1; i<=8; i++) {
	 * System.out.println(conn.getHeaderFieldKey(i) + " : " +
	 * conn.getHeaderField(i)); } conn.disconnect();
	 * 
	 * }
	 */
}
