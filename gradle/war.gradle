apply plugin: "war"

bootWar {
    mainClassName = "com.springboot.rest.BasicSampleApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
